'use strict';

describe('myApp.mngBook module', function() {

  beforeEach(module('myApp.mngBook'));

  describe('mngBook controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('MngBookCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});